# Allow all robots to crawl the app
User-agent: *

# Allow access to all API routes for OG images
Allow: /api/og/*

# Allow standard routes
Allow: /users/*
Allow: /posts/*

# Default allow for other routes
Disallow: /api/*
Disallow: /auth/*

# Sitemap location
Sitemap: https://zling.ru/sitemap.xml
